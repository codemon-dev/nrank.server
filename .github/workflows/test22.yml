name: Hourly cron job
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
       logLevel:
          description: 'Log level'     
          required: true
          default: 'warning'
       tags:
          description: 'Test scenario tags'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: test corn
        run: |
          curl --request POST \
          --url 'http://172.30.1.9:3333/api/hello'
